spring:
    h2:
        console:
            enabled: false
    datasource:
        url: jdbc:{database}://{host}:{port}/{databasename}
        username:
        password:
        driver-class-name:
    jpa:
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate:
                dialect:
